# GitHub图床配置
# GitHub Image Bed Configuration

# GitHub仓库名（必需）
# GitHub repository name (required)
VITE_GITHUB_IMAGE_REPO=zillionare/images

# GitHub分支名（必需）
# GitHub branch name (required)
VITE_GITHUB_IMAGE_BRANCH=main

# GitHub访问令牌（必需）
# GitHub access token (required)
# 需要有仓库写入权限的Personal Access Token
# Requires a Personal Access Token with repository write permissions
VITE_GITHUB_IMAGE_TOKEN=your_github_token_here

# 存储路径模板（可选）
# Storage path template (optional)
# 支持变量：{year} - 年份，{month} - 月份
# Supports variables: {year} - year, {month} - month
VITE_GITHUB_IMAGE_BASE_PATH=images/{year}/{month}/

# 访问基础URL（可选）
# Base URL for accessing images (optional)
VITE_GITHUB_IMAGE_BASE_URL=https://images.jieyu.ai
